# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，版本号采用语义化版本。

## [未发布]

### 新增

- 内置 46 个空调型号的官方 MIoT 规格，并支持按型号在线获取与本地缓存。
- 自动显示米家设备名称；按型号生成模式、风速、温度范围和步长。
- 摆风、ECO、睡眠、辅热、干燥、灯光与提示音按设备能力显示。
- 添加规格刷新脚本、多型号解析与温度校验测试、替代型号离线界面检查。

### 修复

- 高级属性页的灯光亮度、制冷效果和制热效果选项改为等宽铺满，消除右侧留白造成的不对称。
- 运行诊断读数的英文属性名后增加中文释义，保留原属性名便于排查。
- 重新选择空调会重建控制器，避免显示新名称却继续操作旧设备；切换时清除上一台设备的状态、历史图表和待应用温度。
- 开关状态未读到时不猜测为关机；状态属性分组读取，可选属性异常不会遮蔽核心开关状态。
- 托盘开关、手动调温和预设温度也要求已确认的开关状态；开关写入失败后先刷新，避免沿用旧状态再次操作。
- 优先采用正式规格；模式编号有版本冲突的三个型号禁用模式写入，未知电量单位明确标注。
- 在线型号存在多个同级规格版本时也禁用未确认的模式写入；旧规格缓存按保守规则处理。
- 温度回读容差按设备步长调整，写入值按公开格式范围校验。
- 设备拒绝或写入超时后不再换通道重复发送，超时结果标为未确认；输入错误不再计为断线。
- 批量属性读取会隔离多个不兼容属性；整数写入避免精度损失，并修正温度上限与步长不对齐时的取整。
- 规格刷新必须完整成功才覆盖内置清单；重新选择设备前先核实规格可安全使用。
- 状态属性按最多 8 项分批读取，原机型移除新增的非必要故障属性请求，降低整批超时风险。
- 电源写入确认后更新本地状态，读取超时不再导致按钮重复发送开机；在途指令防重入。
- 同一设备重连保留已读电量与状态，连续读取失败退避 30 秒并抑制重复弹窗。

- 属性读写使用当前型号地址，按返回地址匹配响应，避免型号间映射混用。
- 设置保存保留 32°C 和 0.1°C 等型号特有温度值，在下发前按设备规格校验。
- 非原机型不再请求原机型专用耗电历史，未知或不支持的写入明确拒绝。

> 当前为离线规格和界面验证，新增机型尚未逐台真机测试。

## [2.1.0] - 2026-09-21

### 新增

- Windows 单实例保护：再次启动时唤醒已运行的主窗口。
- 关闭确认弹窗，可选择收进系统托盘或直接退出，并能记住偏好。
- 设置页新增关闭行为设置；设置写入改为独立线程，避免磁盘写入影响界面操作。
- 应用内嵌多尺寸 Windows 图标，覆盖可执行文件、主窗口和系统托盘。

### 改进

- 最小化、托盘恢复和窗口恢复后会重新建立软件渲染表面，改善恢复后的绘制可靠性。
- 温度调节、自动刷新和提示条更新减少不必要的界面重建，操作响应更平滑。
- UI 预览示例补充关闭弹窗、系统托盘和单实例相关检查。
- 更新项目介绍、下载方式和发布文档。

## [2.0.1] - 2026-09-20

### 修复

- 修复 Windows 下连续更新目标温度可能停滞的问题。

## [2.0.0] - 2026-09-19

### 新增

- 发布 Rust + Slint 原生桌面客户端，支持米家空调控制、设备诊断、电量数据、扫码登录和托盘运行。

[2.1.0]: https://github.com/zimo888u/mi-jia-kong-tiao/compare/v2.0.1...v2.1.0
[2.0.1]: https://github.com/zimo888u/mi-jia-kong-tiao/compare/v2.0.0...v2.0.1
[2.0.0]: https://github.com/zimo888u/mi-jia-kong-tiao/releases/tag/v2.0.0
